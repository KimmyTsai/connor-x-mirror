# CLAUDE.md

本檔為 Claude Code 的常駐專案背景。修改程式碼前請先讀完。

---

## 專案是什麼

**鏡眼 Mirror Eye** ── 道路反射鏡的安裝與維護系統。

參賽：2026 DevJam TW，Google Cloud 賽道，主題「Agent X 智慧城市」。
四人團隊，24 小時黑客松。

**核心洞見（所有設計都由此推導）**：

> 每一支道路反射鏡的位置，都是過去某個人親自認定「這裡會出事」而標定的。
> 那是一份累積數十年、由人工標註的城市風險地圖，從來沒有人把它當資料用過。
> 而它已經替我們解決了立桿、選址、角度、地權這四件最貴的事。

---

## 已定案的設計決策（不要在未討論的情況下推翻）

### 1. 兩個分數，不是一個

「設置需求」與「維護需求」是不同問題，各自有獨立的視圖與圖層。
合併成單一「需求程度」會讓排序失去意義。

| | 設置需求 | 維護需求 |
|---|---|---|
| 對象 | 沒有鏡子的路口 | 已有鏡子的點位 |
| 視圖 | `v_installation_need` | `v_maintenance_priority` |
| 動作 | 新設工程 | 派工巡修 |

### 2. 維護優先序必須乘上點位風險

```
priority_score = condition_score × (0.5 + risk_score / 100)
```

**理由**：同樣髒的兩面鏡子，在國小通學路口的那面要先修。
只判斷「鏡面有多髒」是影像分類作業，乘上風險才是維運決策系統。
這一行是本專案與純 CV 專案的分界，不要簡化掉。

### 3. 評分必須是加權分項累加，不能是黑箱

評審會點某個排第一的路口問「為什麼是它」。
所有分數都要能拆解回分項貢獻（`s_accident` / `s_sight` / `s_width` / `s_vulnerable`），
前端也要畫成貢獻條。**可解釋性優先於準確率。**

### 4. 去重是必做的

同一支鏡子會被多個全景點、多個方位角重複看到。
不分群的話清冊數字會膨脹三四倍，被評審抓到就毀了。
分群半徑 12 公尺，群心為最終座標，**觀測支持數即信心指標**。

### 5. 民眾回報不直接加進分數

兩個理由：可以灌票；更根本的是**陳情量高的往往是比較懂得使用制度的社區**，
純靠陳情分配資源會讓最需要的地方永遠排在後面 —— 那正是現行制度的缺陷。

正確用法三層：
- 獨立顯示（`report_count` 與客觀分數並列）
- Gemini 抽取結構化事實（時段、族群、危險型態）作為加權修正
- **歧異挖掘**：回報多但分數低 → `flag_review`，排進人工複查

### 6. 街景管廣度，民眾回報管時效

Street View 影像可能是數年前拍的，**不能宣稱「這面鏡子現在是髒的」**。
- 街景 → 建立基線清冊
- 民眾照片 → 即時鏡況
- 兩者差異本身就是劣化訊號

### 7. 兩階段掃描

- Stage 1：8 方位 × `fov=50` × `pitch=8` 廣角掃描，找出方位
- Stage 2：對準該方位 `fov=20` 變焦特寫，供鏡況判讀

Stage 2 直接解決鏡面在廣角圖中只有數十像素的解析度瓶頸。

### 8. 定位：單射線投影

由 Gemini 回傳的 `box_2d` 中心反推方位角，沿方位角投影 `ASSUMED_DIST_M = 8.0` 公尺。
三角交會定位列為 roadmap，24 小時內不做。

`ASSUMED_DIST_M` 應以實地量測校準 —— 這是簡報的加分點。

### 9. 判讀 rubric 的權重理由

| 維度 | 權重 | 理由 |
|---|---|---|
| 角度偏移 | 0.30 | **最危險：駕駛會相信一面錯的鏡子** |
| 鏡面髒污 | 0.25 | |
| 破損缺角 | 0.20 | |
| 遮蔽 | 0.15 | |
| 鏽蝕傾斜 | 0.10 | |

信心 < 0.6 自動 `needs_human_review`（human-in-the-loop，簡報加分項）。

### 10. 偵測必須強制列出證據

三條件同時成立才判 true：獨立桿件 ＋ 鏡面有廣角變形反射影像 ＋ 位於道路旁。
要求 Gemini 說出證據，比只回 true/false 準確得多。

已知誤判源：圓形禁制標誌、車輛後照鏡、圓形招牌時鐘、建築玻璃反射、停車場室內凸面鏡。

---

## 架構原則

> **雲端不在安全迴路上，雲端在學習迴路上。**

即時警示（roadmap 的應用二）必須端側閉合，一秒內完成，不經網路往返。
雲端負責模型訓練與下發、事件彙整、鏡況判讀、維運決策。

### 隱私紅線（不可妥協）

反射鏡位置是住宅巷弄轉角，是人家家門口。
- 影像**只在端側處理、不上傳、不留存**
- 只回傳「幾點幾分有幾台車從哪個方向進入盲區」的 metadata
- 雲端另有 Cloud DLP 作第二層

---

## 技術棧

| 層 | 服務 |
|---|---|
| 判讀 | Gemini 2.5 Flash（量大）/ Pro（陳情書，品質優先） |
| 資料 | BigQuery ＋ BigQuery GIS（`ST_DWithin` 空間關聯） |
| API | Cloud Run ＋ FastAPI |
| 影像 | Street View Static ＋ Metadata API |
| 前端 | Maps JavaScript API ＋ Firebase Hosting |
| 儀表板 | Looker Studio |

**成本控制**：Metadata API 免費，Static API 計費。
**任何取圖前一定先呼叫 Metadata 預檢**，否則會燒掉額度抓回空白圖。

---

## 檔案結構

```
mirror-eye/
├── schema.sql              BigQuery 表與評分視圖
├── pipeline/
│   ├── detect.py           街景偵測鏡子 → 寫入 mirrors
│   ├── inspection.py       鏡況判讀 → 寫入 inspections
│   ├── link.py             把 mirrors 逐一送進 inspect_point()
│   └── validate.py         precision / recall 驗證
├── api/main.py             Cloud Run API
└── web/index.html          地圖前端
```

資料流單向：`detect`（在哪）→ `inspect`（好不好）→ 視圖（優先序）→ API → 網頁。

---

## 開發紀律

### 垂直切片優先

**不要平行開發四個模組最後才整合。** 先打通：

```
一張街景照片 → Cloud Run → Gemini 判讀 → BigQuery → 地圖上一點變紅
```

通了之後才在能動的系統上疊加。

### 先小後大

任何管線變更，**先跑五個路口確認正確再放大**。
不然錯誤會被放大六十倍，而且會在第八小時才發現。

### 驗證協議（拿分關鍵）

40 個路口的人工標註集，跑 `validate.py` 得出 precision / recall。
簡報上要有「precision 0.9x / recall 0.8x，在 40 個路口的人工標註集上」這一行。
**失敗案例各挑一個放進簡報 —— 主動秀失敗會提升評審信任度。**

---

## 比賽評分權重（決定工時分配）

| 評分項 | 佔比 | 拿分關鍵 |
|---|---|---|
| 技術實現與可行性 | 30% | 真資料跑通、實拍驗證、誠實標示原型 |
| Google 技術運用與創新性 | 25% | **服務選型的取捨理由，不是堆服務** |
| 問題針對性與影響力 | 25% | 成本對照表、覆蓋率推估 |
| Demo 展示與技術表達 | 15% | 腳本、練習、備援影片 |
| 雲端架設完整性 | 5% | 能跑就好，**不要碰 Terraform / CI-CD** |

**最後六小時全員停止寫功能**，練 pitch、錄備援影片。
影響力 25% ＋ Demo 15% ＝ 40% 的分數壓在那場簡報上。

---

## 用語紀律

- **原型**（手機跑真實影片做真實偵測）→ 拿分
- **模擬**（自己生假資料餵自己的系統）→ 扣分

不要說「我們模擬了路側裝置」，
要說「端側運算目前跑在手機上，換成嵌入式模組是工程問題，不是技術問題」。

---

## 環境

Windows / PowerShell。環境變數用 `$env:` 語法，不是 `export`：

```powershell
$env:GOOGLE_CLOUD_PROJECT = "..."
$env:GOOGLE_CLOUD_LOCATION = "asia-east1"
$env:MAPS_API_KEY = "..."
```

專案應放在 `C:\dev\mirror-eye`，**避開 OneDrive 與中文路徑**。

`pipeline/validate.py` 會 `import detect`，執行時須在 `pipeline/` 目錄內。

---

## 待辦

- [x] `pipeline/link.py`：把 `mirrors` 表的鏡子逐一送進 `inspect_point()`
- [ ] 驗證 Gemini `box_2d` 的座標順序是否為 `[ymin, xmin, ymax, xmax]`
- [ ] 以實地量測校準 `ASSUMED_DIST_M`
- [ ] 成本對照表填入實際造價數字
- [ ] Stage 2 變焦特寫接進 `inspection.py`
- [ ] 選配：偵測結果輸出成 OSM 格式回饋開放社群
